name: Schedule Probe

on:
  workflow_dispatch:
  schedule:
    - cron: "* * * * *"

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Print trigger and time
        run: |
          echo "Event: ${{ github.event_name }}"
          date -u
